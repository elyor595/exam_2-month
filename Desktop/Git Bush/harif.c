#include <stdio.h>

int main (){
	char a;
	scanf(" %c",&a);
	if ('a' == a || 'e' == a ||'u' == a || 'i' == a ||'o' == a){
	printf("%c Unli xarf",a);
}
	else {
	printf("%c undosh xarf");
}
   return 0;
}
